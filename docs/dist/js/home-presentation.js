var store = {
  visible: false
};

const toggleVisibility = () => {
  store.visible = !store.visible;
  updateUI();
};

const updateUI = () => {
  if(store.visible) {
    
  }
  else {

  }
}

export default toggleVisibility;
